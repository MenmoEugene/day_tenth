//自定义异常

class Demo
{
	int div(int a,int b)throws FuShuException  //throws 用在函数上（小括号和大括号之间）可以跟多个异常类，需要逗号隔开
	{
		if(b<0)
		          throw new FuShuException("出现了负数"，b)//  手动通过throw关键字抛出一个自定义异常  跟的是异常对象
		return a/b;
	}
}